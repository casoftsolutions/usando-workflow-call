name: Deploy no K8s
on:
    workflow_call:
        inputs:
            ambiente:
              type: string
              required: true

jobs:
    deploy-k8s:
        runs-on: ubuntu-latest
        steps: 
        - name: Obtendo o código
          uses: actions/checkout@v4.1.5
        - name: Configuração de contexto do Kubernetes
          run: echo "${{ inputs.ambiente}} Configuração de contexto do Kubernetes realizada com sucesso!"